#include <stdio.h>

/** \brief pide un numero y lo valida en un rango determinado
 *
 * \param [] char texto que se va a mostrar
 * \param int minimo valor dentro del rango
 * \param int maximo valor dentro del rango
 * \return int el numero ingresado, [1] en caso de exito, [0] en caso de error
 *
 */

int pedirEntero(char [], int, int);
